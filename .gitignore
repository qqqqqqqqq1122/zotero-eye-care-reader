# ---- 依赖与构建产物 ----
# 体积大，且都能从源码重建，不进仓库
node_modules/
step010_reader_src/
step060_app/reader-build/
step090_dist/

# ---- 运行/构建日志 ----
*.log

# Edge 在 edge://extensions 里「打包扩展」生成的 .crx。
# 不发布：Windows 上 Edge/Chrome 会拦截非商店来源的 .crx 安装，
# 用户应该用「加载解压缩的扩展」。
*.crx
*.pem

# ---- 系统与编辑器 ----
Thumbs.db
desktop.ini
.vscode/
.idea/

# 注意：step110_edge_ext/reader/ 是刻意保留的 ——
# 那是扩展能直接加载所必需的构建产物（11.7 MB）。
# 用户 clone 下来就能在 edge://extensions 里加载，不必自己跑一遍构建。
